interface:
  display_name: "Manage Skills"
  short_description: "Detect and install host skills"
  default_prompt: "Use $manage-skills to detect the available skill installer, find matching skill namespaces, and install the selected skills."
