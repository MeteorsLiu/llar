import "os"

id "test/testhook"

fromVer "1.0.0"

onBuild ctx => {
	ctx.setMetadata "-lHOOK"
}

onTest ctx => {
	installDir := ctx.outputDir
	os.writeFile(installDir+"/ontest.stamp", []byte("ran"), 0o644)!
}
