id "madler/zlib"

fromVer "v1.3.1"

onBuild ctx => {
	installDir := ctx.outputDir

	a := autotools.new(ctx.SourceDir, ctx.SourceDir+"/_build", installDir)

	a.configure "--static"
	a.build
	a.install

	ctx.setMetadata "-lz"
}
